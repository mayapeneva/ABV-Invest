@page
@model SetPasswordModel
@{
    ViewData["Title"] = "Избери парола";
    ViewData["ActivePage"] = ManageNavPages.ChangePassword;
}

@Html.Partial("_StatusMessage", Model.StatusMessage)
<div class="container bg-abvdarkgreen">
    <div class="col-md-6">
        <h4 class="bg-abvmiddlegreen">@ViewData["Title"]</h4>
        <hr />
        <p class="text-info">
            Вие нямате потребителско име и парола за този сайт. Регистрирайте се, за да имате достъп до сайта.
        </p>
        <div class="row">
            <div class="col-md-6">
                <form id="set-password-form" method="post">
                    <div asp-validation-summary="All" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Input.NewPassword">Нова парола</label>
                        <input asp-for="Input.NewPassword" class="form-control" />
                        <span asp-validation-for="Input.NewPassword" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Input.ConfirmPassword">Потвърди новата парола</label>
                        <input asp-for="Input.ConfirmPassword" class="form-control" />
                        <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn bg-bg-abvdarkgreen bg-abvlightgreen">Избери парола</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}